<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login - Attendlog</title>
    <link rel="stylesheet" href="/static/index.css">
</head>
<body class="font-inter">
    <h1 class="font-medium text-4xl mt-20 text-center">
        Attendlog
    </h1>

    <form class="mt-5 px-5 space-y-5">
        <fieldset class="form-control w-full max-w-xs">
            <label for="email" class="label">
                <span class="label-text">Email Address</span>
            </label>
            <input id="email" type="email" placeholder="Please enter a valid email" class="input input-bordered w-full max-w-xs" />
        </fieldset>

        <fieldset class="form-control w-full max-w-xs">
            <label for="password" class="label">
                <span class="label-text">Password</span>
            </label>
            <input id="password" type="password" placeholder="Please enter a strong password" class="input input-bordered w-full max-w-xs" />
        </fieldset>

        <div class="w-full">
            <button class="btn w-full bg-black text-white">
                Login
            </button>
        </div>

        <p class="text-center">
            Are you new here?
            <a class="link link-primary" href="/auth/signup">
                Sign Up
            </a>
        </p>
    </form>
</body>
</html>